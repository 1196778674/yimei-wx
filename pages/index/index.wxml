<view class="container">
  <view class="list">
    <view class="item c{{item.id+1}}" wx:for="{{list}}" wx:key="{{item.id}}" data-id="{{item.id}}" bindtap="goProject">
      {{item.title}}
    </view>
  </view>
</view>